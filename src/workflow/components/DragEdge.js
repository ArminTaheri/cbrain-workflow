import React from "react";
import PropTypes from "prop-types";
import { PointType } from "./types";

const DragEdge = undefined;

DragEdge.propTypes = {
  start: PointType,
  end: PointType
};

export default DragEdge;
