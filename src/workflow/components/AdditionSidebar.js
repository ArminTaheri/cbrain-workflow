import React from "react";
import PropTypes from "prop-types";

// Sidebar component for working with a single node.
const AdditionSidebar = undefined;

AdditionSidebar.propTypes = {
  onAddTask: PropTypes.func,
  onAddFileSelection: PropTypes.func
};

export default AdditionSidebar;
