import React from "react";
import { GraphType } from "../types";

// GUI Layer containing the visual graph representation
// of the workflow.
const GraphLayer = undefined;

GraphLayer.propTypes = {
  graph: GraphType.isRequired
};
export default GraphLayer;
